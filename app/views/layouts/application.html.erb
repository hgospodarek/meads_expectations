<!DOCTYPE html>
<html lang="en">
<head>
  <title>Meads Expectations</title>
<%= stylesheet_link_tag 'application', media: 'all' %>
<%= javascript_include_tag 'application' %>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0, user-scalable=no"/>
<%= csrf_meta_tags %>
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>

<body>
  <div class="navbar-fixed">
    <nav class="white" role="navigation">
      <div class="nav-wrapper">
        <ul class="left hide-on-med-and-down">
          <% if user_signed_in? %>
            <li><%= link_to 'Recipes', recipes_path, class: "black-text" %></li>
            <li><%= link_to 'Batches', batches_path, class: "black-text" %></li>
          <% end %>
        </ul>
        <%= link_to (image_tag 'noun_18918_cc.svg', class: "bee"), root_path, class: "brand-logo center" %>
        <ul class="right hide-on-med-and-down">
          <% if user_signed_in? %>
            <li><%= link_to 'Settings', edit_user_registration_path, class: "black-text" %></li>
            <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "black-text" %></li>
          <% else %>
            <li><%= link_to 'Sign Up', new_user_registration_path, class: "black-text" %></li>
            <li><%= link_to 'Sign In', new_user_session_path, class: "black-text" %></li>
          <% end %>
        </ul>
        <ul id="nav-mobile" class="side-nav">
          <% if user_signed_in? %>
            <li><%= link_to 'Recipes', recipes_path, class: "black-text" %></li>
            <li><%= link_to 'Batches', batches_path, class: "black-text" %></li>
          <% end %>
          <% if user_signed_in? %>
            <li><%= link_to 'Settings', edit_user_registration_path %></li>
            <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to 'Sign Up', new_user_registration_path %></li>
            <li><%= link_to 'Sign In', new_user_session_path %></li>
          <% end %>
        </ul>
        <a href="#" data-activates="nav-mobile" class="button-collapse"><i class="material-icons black-text">menu</i></a>
      </div>
    </nav>
  </div>

  <% flash.each do |type, message| %>
  <% type = 'success' if type == 'notice' %>
  <div class="flash flash-<%= type %>"
       data-type="<%= type %>"
       data-message="<%= message %>">
    <%= message %>
  </div>
<% end %>

  <div>
    <%= yield %>
  </div>

</body>
</html>
